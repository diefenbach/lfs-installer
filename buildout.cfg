[buildout]
parts =
    django

find-links =
    http://pypi.iqpp.de/

versions =
    versions

eggs =
    django-lfs

[versions]
django = 1.6.5
djangorecipe = 1.9
django-lfs = 0.9.0a1

[django]
recipe =
    djangorecipe

eggs =
    ${buildout:eggs}

project =
    lfs_project

settings=
    settings

extra-paths =
    ${buildout:directory}/lfs_project
